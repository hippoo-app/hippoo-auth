name: Release to WordPress.org

on:
  push:
    branches: [main]

jobs:
  release:
    uses: hippoo-app/devops-templates/.github/workflows/deploy-to-wporg.yml@main
    with:
      plugin-slug: hippoo-auth
    secrets:
      SVN_USERNAME: ${{ secrets.WPORG_SVN_USERNAME_HIPPOOSUPPORT }}
      SVN_PASSWORD: ${{ secrets.WPORG_SVN_PASSWORD_HIPPOOSUPPORT }}
